{
    "version": "0.1.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Test Launch",
            "program": "${workspaceFolder}/dist/test.js",
            "preLaunchTask": "tsc: build - tsconfig.json",
            "args": [
                "--trace-warnings"
            ]
        }
    ]
}